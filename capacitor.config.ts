import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.iocare.puneriawaaz',
  appName: 'Puneri Awaaz',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
